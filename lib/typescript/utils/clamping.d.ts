export declare const clamp: (value: number, min: number, max: number) => number;
export declare const rubberBandClamp: (x: number, coeff: number, dim: number) => number;
export declare const withRubberBandClamp: (x: number, coeff: number, dim: number, limits: [number, number]) => number;
